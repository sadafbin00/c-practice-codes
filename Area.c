#include<stdio.h>
int main()
{
int length, width, area;
printf("Enter a length value: %d\n");
scanf("%d",&length);
printf("Enter a width value: %d\n");
scanf("%d",&width);
area=length*width;
printf("Area of this value = %d\n",area);
scanf("%d","%d",&length,&width);
return 0;
}
